import { isCapable } from 'ga-wdi-boston.ember-auth/helpers/is-capable';
import { module, test } from 'qunit';

module('Unit | Helper | is capable');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isCapable([42]);
  assert.ok(result);
});
