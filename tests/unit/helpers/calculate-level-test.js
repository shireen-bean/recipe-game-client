import { calculateLevel } from 'ga-wdi-boston.ember-auth/helpers/calculate-level';
import { module, test } from 'qunit';

module('Unit | Helper | calculate level');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = calculateLevel([42]);
  assert.ok(result);
});
