import { forLoop } from 'ga-wdi-boston.ember-auth/helpers/for-loop';
import { module, test } from 'qunit';

module('Unit | Helper | for loop');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = forLoop([42]);
  assert.ok(result);
});
